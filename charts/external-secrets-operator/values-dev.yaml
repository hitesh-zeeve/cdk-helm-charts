# Example values for development environment
# Note: Install the External Secrets Operator separately first with IRSA configured

# Development Environment
aws:
  region: "us-east-2"

# ServiceAccount reference (must match the installed operator)
external-secrets:
  serviceAccount:
    name: external-secrets-sa
