apiVersion: v2
name: postgres
description: A Helm chart to deploy a PostgreSQL instance with optional database initialization

# Application chart
type: application

version: 0.1.0
appVersion: "16.0"
