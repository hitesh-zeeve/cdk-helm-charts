{{- if .Values.externalSecrets.enabled }}
---
# Shared L1 RPC URL (may be used by multiple components)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "bridgeUI.fullname" . }}-shared-l1
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "bridgeUI.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval }}
  secretStoreRef:
    kind: {{ .Values.externalSecrets.storeRef.kind }}
    name: {{ .Values.externalSecrets.storeRef.name }}
  
  target:
    name: {{ include "bridgeUI.fullname" . }}-shared-l1
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        l1RpcURL: "{{ `{{ .l1RpcURL }}` }}"
  
  dataFrom:
    - extract:
        key: {{ .Values.externalSecrets.awsSecrets.sharedL1RpcUrl }}
{{- end }}
